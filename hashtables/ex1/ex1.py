def get_indices_of_item_weights(weights, length, limit):
    """
    YOUR CODE HERE
    """
    # weights is the array of weights, 
    # length determines amount of weights in the weights array
    # limit is the target value
    # given the weights array find a combination of weights to equal the target value aka limit
    # must return index
    cache = {}
    
        
    

    return None
